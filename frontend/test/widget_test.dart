import 'package:flutter_test/flutter_test.dart';
import 'package:smartear_flutter/main.dart';

void main() {
  test('app can be constructed', () {
    expect(const SmartEarApp(), isA<SmartEarApp>());
  });
}
